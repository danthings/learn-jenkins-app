pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh "pwd"
                sh "ls -l"
                sh "whoami"
            }
        }
    }
}
